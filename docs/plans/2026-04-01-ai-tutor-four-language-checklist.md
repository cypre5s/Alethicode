# AI 导学题目四语言开放与自适应补全执行清单

更新时间：2026-04-01

## 执行约束

- [x] 范围固定为“所有编程题（题库题 + 审核通过的 AI 题）”，`student_private` 题除外
- [x] 目标语言固定为 `Python3 / C / C++ / Java`
- [x] 不新增数据库迁移，仅做查询与运行时归一化
- [x] 缺失模板时使用标准 IO 启动模板，不做跨语言翻译

## 实施清单

- [x] 新增后端语言归一化测试，先验证 AI 导学题会扩展为四语言
- [x] 实现后端统一语言/模板归一化能力
- [x] 将题目详情接口接入统一归一化结果
- [x] 将提交接口接入统一归一化结果
- [x] 将调试接口接入统一归一化结果
- [x] 新增前端源码契约测试，先验证四语言下拉和自适应补全约束
- [x] 实现做题页四语言展示与切换
- [x] 实现编辑器四语言自动补全 source 切换
- [x] 更新本轮变更到 `CHANGELOG.md`
- [x] 跑完后端与前端目标测试并记录结果

## 备注

- 本文档用于执行过程打勾；每完成一项立即更新，不在结束时一次性回填。

## 增补：AI 导学范围判定修正（2026-04-01）

- [x] 将题目详情 `ai_tutor_enabled` 判定改为复用统一语言归一化器，不再仅按 `visibility_status` 判定
- [x] 覆盖“题库内客观题不应开启 AI 导学”的后端契约测试
- [x] 运行后端目标测试并记录通过结果
- [x] 将本次修正写入 `CHANGELOG.md`

## 增补：三处收口缺口补齐（2026-04-01）

- [x] 提交流程去掉语言静默回退，`language` 缺失改为 fail-fast
- [x] AC 后代码质量评估触发从 `Python3 only` 改为随当前提交语言触发
- [x] 题目页保留后端返回语言顺序，不再对 `problem.languages` 做排序
- [x] 补充后端集成测试覆盖缺失 `language` 与 `C++` 代码质量评估
- [x] 补充前端契约测试覆盖“默认语言不受前端排序干扰”
- [x] 将本次收口修正写入 `CHANGELOG.md`

## 增补：复核结论前基线修正（2026-04-01）

- [x] 修复不存在接口 `/api/judge/review-queue` 被全局异常处理误转为 500 的问题（应返回 404）
- [x] 补齐 `SubmissionJudgeThrottleIntegrationTest` 的异步代码质量断言时序，避免偶发 `pending` 误判
- [x] 重新执行 `SubmissionJudgeThrottleIntegrationTest` 全量并确认通过
- [x] 将本次修正写入 `CHANGELOG.md`

## 增补：语言切换清空 AI 对话（2026-04-01）

- [x] 删除管理端 `judge/review-queue` 残留 API 封装，避免继续调用已废弃接口
- [x] 做题页语言切换接入确认弹窗：存在 AI 导学内容时提示“切换后将清空当前 AI 导学对话记录”
- [x] 语言切换前统一清空 workflow 会话与聊天缓存，切换后重新创建会话
- [x] 修正编辑器语言切换为父组件受控模式，避免取消切换后高亮/补全状态漂移
- [x] 补充前端契约测试覆盖“切语言清空对话提示”和“受控语言切换”约束
- [x] 将本次修正写入 `CHANGELOG.md`

## 增补：Python 专属字段残留清理（2026-04-01）

- [x] 清理骨架生成解析中的 `python_skeleton` 专属字段，统一为通用字段协议
- [x] 将本次修正写入 `CHANGELOG.md`

## 增补：多语言回归 SQL 修复（2026-04-01）

- [x] 修复 workflow 题目加载 SQL 在语言包 join 后 `id` 字段歧义导致的 500
- [x] 重新执行 workflow 关键集成测试并确认通过
- [x] 将本次修正写入 `CHANGELOG.md`

## 增补：教室 AI 生成题入库模板一致性（2026-04-01）

- [x] 修复 `ClassroomServiceImpl#aiGeneratedProblemPublish`：编程题发布入库时不再写空 `template`，改为直接写入四语言完整模板
- [x] 将本次修正写入 `CHANGELOG.md`
